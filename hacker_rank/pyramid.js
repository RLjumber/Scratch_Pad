// my own problem, lets write a standard pyramid.


function pyramid(n) {
    // given a height n, return a pyramid of hashes where the base is equal to the height n
  
    count = 1;

    while(count <= n) {
        let hash = "";
        numberOfHashes = count;

        console.log(count)
        count ++;
    }
}

console.log(pyramid(20))